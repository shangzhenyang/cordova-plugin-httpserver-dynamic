allprojects {
    repositories {
        jcenter()
        maven { url "https://jitpack.io" }
    }
}
dependencies {
    implementation ('com.github.jh3141:nanohttpd:patched-SNAPSHOT') {
        exclude module: 'nanohttpd-webserver-markdown-plugin'
        exclude group: 'org.parboiled'
    }
}
